/// The kubelet manages pod lifecycle on this node.
pub struct Kubelet {
    // TODO: store handle, runtime handle
}

impl Kubelet {
    /// Run the kubelet sync loop.
    pub async fn run(&self) -> anyhow::Result<()> {
        todo!("Phase 4: implement kubelet")
    }
}
